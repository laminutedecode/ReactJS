// Redux :
// Redux est une bibliothèque de gestion d'état qui propose un flux de données unidirectionnel. Il utilise un store centralisé, des actions (intentions de modification de l'état), et des reducers (fonctions qui décrivent comment l'état change).

// React-Redux :
// React-Redux est une intégration de Redux avec React. Le <Provider> enveloppe l'application pour rendre le store Redux accessible. La fonction connect connecte les composants React au store, permettant d'accéder et de modifier l'état global de manière simplifiée.
import './App.css'

function App() {
  

  return (
    <>
      
    </>
  )
}

export default App
