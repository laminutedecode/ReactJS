

const initialState = {
  counter: 0,
  user: {
    username: 'JohnDoe',
    email: 'john.doe@example.com'
  }
};

const rootReducer = (state = initialState) => {
      return state;
  }


export default rootReducer; 
